@ echo off
REM need to extract the first two lines of the output from ssh-agent and slap a set before them then run that as a command, then run ssh-add and we should be golden
echo setup-ssh.bat has yet to be implemented
